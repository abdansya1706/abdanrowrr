Portofolio Animations

- Animasi utama berbasis CSS keyframes.
- Elemen yang memiliki class `.reveal` akan dianimasikan saat masuk viewport menggunakan IntersectionObserver di `template/index.html`.
- Jika user mengaktifkan `prefers-reduced-motion: reduce`, animasi reveal akan di-skip.

